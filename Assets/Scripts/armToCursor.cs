using UnityEngine;
using System.Collections;

public class armToCursor : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		/*mouse = Camera.main.ScreenToWorldPoint(Input.mousePosition);
		arm.animation.transform.LookAt(new Vector3(mouse.x, mouse.y, 0.0f));*/
	}
}
